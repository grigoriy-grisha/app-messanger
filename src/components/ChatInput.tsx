import styled from "styled-components";

const ChatInputContainer = styled.div`
  width: 100%;
  height: 7vh;
  padding: 15px 25px;
`

export const ChatInput = () => {
  return (
    <ChatInputContainer>

    </ChatInputContainer>
  )
}